<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:wght@200&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <title>Взувайка</title>
    <style>
        body {
            background-color: #f2f2f2;
            font-family: Arial, sans-serif;
        }

        h1.title {
            font-size: 3em;
            text-align: center;
            color: aliceblue;
            text-transform: uppercase;
            margin: 0;
            padding: 1em 0;
            background-color: #333;
        }

        .Goods {
            font-size: 2em;
            font-weight: bold;
            text-align: center;
            margin: 1em 0;
        }

        .shop {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            padding: 0 1em;
        }

        .shop-item {
            width: 30%;
            margin: 1em 0;
            background-color: white;
            box-shadow: 0px 2px 6px rgba(0, 0, 0, 0.2);
            border-radius: 5px;
            overflow: hidden;
        }

        .shop-item a {
            text-decoration: none;
        }

        .shop-item img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .shop-item ul {
            list-style: none;
            margin: 1em;
            padding: 0;
        }

        .shop-item ul li p {
            margin: 0;
            padding: 0;
        }


        .item-but {
            display: block;
            width: 100%;
            padding: 1em 0;
            background-color: #333;
            color: white;
            border: none;
            cursor: pointer;
        }

        .item-but:hover {
            background-color: #4CAF50;
        }
    </style>
</head>
<body>

<h1 class="title" style="text-align: center;color: aliceblue;text-transform: uppercase">COOL SHOP</h1>

    <div class="Goods">Our goods</div>
    <div></div>
</div>

<div class="shop">
    {% for item in shoes %}
        <div  class="shop-item">
            <img src="{{ item.image }}" alt="shoes-image">
            <ul>
                <li><p style="margin-bottom: 20px; font-weight: bold">{{ item.title }} </p></li>
                <li><p style="margin-bottom: 25px; display: -webkit-box; -webkit-box-orient: vertical;  overflow: hidden;text-overflow: ellipsis;-webkit-line-clamp: 3; ">{{ item.description | slice(0, 300) }} </p></li>
                <li><p style=" font-weight: bold;">Price: {{ item.price }} UAH </p></li>

            </ul>
            <a  href="{{ path('shop_item', {'id': item.id}) }}">
                <button class="item-but">BUY</button>
            </a>
        </div>

    {% endfor %}
</div>
</body>
</html>
